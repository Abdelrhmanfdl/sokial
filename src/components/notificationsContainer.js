import {
  TextField,
  TextareaAutosize,
  Grid,
  Button,
  Dialog,
  DialogTitle,
  DialogContent,
  DialogActions,
} from "@material-ui/core";

const NotificationsContainer = (props) => {};

export default NotificationsContainer;
